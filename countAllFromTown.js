module.exports = function (regNum, regs) {
    var reg=regNum.split(',').map(item=>item.trim());;
    var count=0;
    for(var i=0;i<reg.length;i++){
      
      if(reg[i].startsWith(regS)) count++;{
       
            }
    }  
     return count; 
   
  }
 
